- name: tests
  service: web
  command: lein midje

## Production
- name: Push to GCR (production)
  tag: master
  service: web
  type: push
  image_name: gcr.io/ovation-io/webapi
  image_tag: "production-{{ .Timestamp }}"
  registry: https://gcr.io
  dockercfg_service: gcr_dockercfg

- name: Deploy (production)
  tag: master
  service: gke-deploy
  command: deploy/deploy.sh production

## All others
- name: Push to GCR
  tag: ^(staging|development)
  service: indexer
  type: push
  image_name: gcr.io/ovation-io/indexer
  image_tag: "{{ .Branch }}-{{ .Timestamp }}"
  registry: https://gcr.io
  dockercfg_service: gcr_dockercfg\

- name: Deploy
  tag: ^(staging|development)
  service: deployment
  command: "/bin/bash -c 'deploy/deploy.sh $CI_BRANCH'"

